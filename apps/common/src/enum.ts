export enum RoleDataPermType {
  all = "all",
  custom = "custom",
  department = "department",
  departments = "departments",
  user = "user",
}

/**
 * 账号类型
 */
export enum AccountType {
  platform = "platform",
}
