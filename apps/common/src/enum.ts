export enum RoleDataPermType {
  all = "all",
  custom = "custom",
  department = "department",
  departments = "departments",
  user = "user",
}

/**
 * 账号类型
 */
export enum AccountType {
  platform = "platform",
}

export enum UploadType {
  local = "local",
  qiniu = "qiniu",
  oss = "oss",
  cos = "cos",
}

export enum FileMime {
  img = "img",
  video = "video",
  audio = "audio",
}
